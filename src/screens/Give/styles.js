import { StyleSheet } from "react-native"

export const createStyles = (colors) => {
    return StyleSheet.create({
        btnContainer: {
            borderRadius: 10,
            marginTop: '10%',
            width: "80%"
        }
    })
}